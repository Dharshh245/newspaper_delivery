apiVersion: v1
kind: Service
metadata:
  name: newspaper-service
  labels:
    app: newspaper-app
spec:
  type: LoadBalancer
  selector:
    app: newspaper-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
